# jsonl-2-json-array README

Dead simple extension to convert a JSON Lines document to JSON Array. One can then use other VSCode json tools to format, explore etc. the document. 

Only command is 

`JSON Lines 2 JSON Array`